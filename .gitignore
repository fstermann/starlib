.venv
.env
.cache
.app-support
.oauth_cache.json
*.py[co]
ca.crt
.DS_Store

# Essentia model weights
*.pb
*.onnx
.vscode
.agents/skills/shadcn
skills-lock.json

# Desktop / Tauri build outputs
desktop/node_modules/
desktop/src-tauri/binaries/starlib-backend*
desktop/src-tauri/target/
# Bundled ffmpeg binary (downloaded at build time, not committed)
desktop/bin/ffmpeg
# PyInstaller build artefacts
build/
dist/
*.spec.bak

# Rust / Cargo workspace artefacts (non-desktop)
target/

# Zensical docs build output
site/

# Alembic dev scratch DB (used by `alembic check` / `revision --autogenerate`)
.alembic-scratch.db

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/
.claude/
